Jenkinsfile (Declarative Pipeline)
/* Requires the Docker Pipeline plugin */
pipeline {
    agent { 
        node {
            label 'learnn'
        }
    }
    stages {
        stage('Build') {
            steps {
                echo 'what is this'
            }
        }
    }
}
